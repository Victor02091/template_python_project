# {{ project_name }}

{{ description }}

## 🛠️ Setup

### 0. Install UV

If not already, install astral uv.

On Linux :

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

On Windows :

```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

More info on [official website](https://docs.astral.sh/uv/getting-started/installation/#__tabbed_1_1).

### 1. Install project and dependencies

Install dependencies and build project :

```bash
uv sync
```

### 2. Manage packages

This project uses the modern `uv` project workflow.

**Add a package:**
```bash
uv add <package_name>
# Examples: uv add pandas / uv add pytest --dev
```

**Remove a package:**
```bash
uv remove <package_name>
```

> **⚠️ Important:** Do **not** use legacy commands like `uv pip install` or manually create environments with `uv venv`.
> The `uv add/remove` commands automatically update `pyproject.toml`, the `uv.lock` file, and your virtual environment in sync.

{% if add_pre_commit -%}
### 3.🪝 Pre-commit

This project uses pre-commit hooks to ensure code quality before every commit.

1. Install the git hook (only needed once):

```bash
uv run pre-commit install
```

2. Run manually on all files (optional):

```bash
uv run pre-commit run --all-files
```

{% endif -%}
## 🚀 Start app

Run main.py entrypoint with the command:

```bash
uv run start-app
```

## 📂 Project Structure

```text
.
├── notebooks/
│   └── my_notebook.ipynb                  # Example notebook
├── src/
│   └── {{ project_slug }}/               # Main Python package
│       ├── core/                         # App configuration and shared constants
│       │   ├── config.py                 # Pydantic settings
│       │   ├── constants.py              # Project constants
│       │   └── logging.py                # Logging configuration
│       ├── __init__.py
│       ├── hello_world.py                # Example module
│       └── main.py                       # Entrypoint used by `uv run start-app`
├── tests/                                # Pytest test suite
│   ├── conftest.py
│   └── test_main.py
├── .python-version                       # Python version pin
├── README.md
├── pyproject.toml                         # Metadata, dependencies, tool config
└── uv.lock                                # Locked dependency graph
```

### Other configuration files

{% if add_pre_commit -%}
- `.pre-commit-config.yaml`: Pre-commit checks run before each commit.
{% endif -%}
{% if add_ci and ci_provider == 'github' -%}
- `.github/workflows/ci.yml`: CI workflow for linting, type checking, and tests.
{% elif add_ci and ci_provider == 'gitlab' -%}
- `.gitlab-ci.yml`: CI pipeline for linting, type checking, and tests.
{% elif add_ci and ci_provider == 'bitbucket' -%}
- `bitbucket-pipelines.yml`: CI pipeline for linting, type checking, and tests.
{% endif -%}
{% if add_docker -%}
- `Dockerfile`: Container build recipe.
- `.dockerignore`: Excludes unnecessary files from Docker build context.
{% endif -%}
{% if add_docs -%}
- `mkdocs.yaml`: Documentation site configuration.
- `docs/index.md`: Documentation homepage.
- `docs/gen_ref_pages.py`: Script to generate API reference pages.
{% endif -%}

## 🧪 Tests

Write your tests in the [tests](./tests) folder. Ensure all test files are named starting with `test_` (e.g., `test_main.py`).

To run all tests, use the command:

```bash
uv run pytest
```

For a visual interface, use the Testing tab in VS Code to run or debug specific tests.

## 🧹 Linting

Check the code for style issues and errors using Ruff:

```bash
uv run ruff check src tests
```

## 🎨 Formatting

Format the code, sort imports, and fix linting issues automatically:

```bash
uv run ruff check --fix && uv run ruff format
```

{% if add_type_checking -%}
## 🛡️ Type Checking

Run static type checking using {{ "Mypy" if type_checker == "mypy" else "Astral Ty" }}:

```bash
uv run {{ "mypy" if type_checker == "mypy" else "ty check" }}
```

{% endif -%}
## ⚙️ Environment variables

Please define and import all environment variables in your code from [config.py](./src/{{ project_slug }}/core/config.py) file like so: 

```python
class Settings(BaseSettings):
    """Load environment variables as settings."""
    
    my_env_variable_1: str # MY_ENV_VARIABLE_1
    my_env_variable_2: int = 5 # MY_ENV_VARIABLE_2
```

During local development, use the .env file to set environment variables.

{% if add_docker -%}
## 🐳 Docker

Build the Docker image:

```bash
docker build -t {{ project_slug }} .
```

Run the container:

```bash
docker run -it --rm {{ project_slug }}
```

{% endif -%}
{% if add_docs -%}
## 📚 Documentation

This project uses [MkDocs](https://www.mkdocs.org/) with [Material for MkDocs](https://squidfunk.github.io/mkdocs-material/).

### Build documentation

```bash
uv run --group docs mkdocs build --clean
```

### Run documentation server

Run the documentation server locally:

```bash
uv run --group docs mkdocs serve
```

Open your browser at `http://127.0.0.1:8000`.

{% endif -%}
## 📧 Contacts

* **{{ author_name }}** - [{{ author_email }}](mailto:{{ author_email }})

